exporter:
  port: 9090

http:
  url:
    # 如果不是 443和80 URL+指定PORT,支持写多个
    - "https://www.baidu.com:443"
    - "http://39.105.137.222:8089"
tcp:
  server:
    # 支持多个 tcp
    - "10.0.0.200:2049"
icmp:
  targets:
    - "8.8.8.8"
    - "192.168.1.1"
